\chapter{Projekt}
\label{chap:projekt}

\lipsum[1-3]

% porownanie opencv vs fpga?

%kamera usb?

% wejscie -> frame diff ->(bg model?) -> median -> | cpu | -> scale? -> index? (every n frames?) -> save to file -> webserver